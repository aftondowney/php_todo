<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
  </head>
  <body>
    <div class="container">
      <h1>Welcome to Your To Do List</h1>
      <p><em>You better do it otherwise your future-self will be pissed!</em></p>

      <form action="/categories" method="post">
          <label for="name">Category name:</label>
          <input id="name" name="name" type="text">

          <button class= "btn btn-success" type="submit">Add category</button>
      </form>

      <form action="/delete_categories" method="post">
          <button class= "btn btn-danger"type="submit">Nuke categories</button>
      </form>

      {% if categories is not empty %}
          <p>Here are all your categories:</p>
          <ul>
              {% for category in categories %}
                  <li><a href="/categories/{{ category.getId }}">{{ category.getName }}</a></li>
              {% endfor %}
          </ul>
      {% else %}
        <p>There are no categories yet!</p>
      {% endif %}

    </div>
  </body>
</html>
